//Юный путешественник решил изучить схему авиационного сообщения Схема авиационного сообщения задана в текстовом файле с именем FileName.
// в виде матрицы смежности.Первая
//строка файла содержит количество городов(n) n <= 15, связанных авиационным сообщением, а
//следующие n строк хранят матрицу(m), m[i][j] = 0, если не имеется возможности перелета из города i в город j, иначе m[i][j] = 1. 
// Определить номера городов, в которые из города K можно долететь не менее чем с L пересадками 
// и более коротких путей к таким городам не существует.Перечислите номера таких городов в порядке возрастания.Нумерация городов начинается с 1.
// Если таких городов нет, выведите число(-1).
//
//https://chatgpt.com/

#include "Header.h"
#include <iostream>

//FIX ME: Убрала глобальное использование std
//FIX ME: Разделила на модули
//FIX ME: Перенесла все фигурные скобки на новую строку
//FIX ME: Переименовала некоторые названия функций и переменных
//FIX ME: Изменила пользовательский интерфейс
//FIX ME: Дополнила функции проверок
//FIX ME: Добавила необходимые библиотеки

int main()
{
    setlocale(LC_ALL, "Russian");

    const std::string FileName = "FileName.txt";

    // Создаем и загружаем граф
    AirGraph graph;

    if (!graph.LoadFromFile(FileName))
    {
        return 1;
    }

    // Ввод данных от пользователя
    int K, L;

    std::cout << "Введите город K (1-" << graph.CityCount << "): ";
    std::cin >> K;

    std::cout << "Введите минимальное число пересадок L: ";
    std::cin >> L;

    // Проверка корректности ввода
    if (!AirPathFinder::ValidateInput(graph.CityCount, K, L))
    {
        return 1;
    }

    // Поиск кратчайших путей
    AirPathFinder pathFinder(graph);
    int StartCity = K - 1; // Приводим к нумерации с 0

    pathFinder.FindShortestPaths(StartCity);

    // Получение результата
    std::vector<int> Result = pathFinder.GetCitiesWithMinTransfers(StartCity, L);

    // Вывод результата
    PrintResult(Result);

    return 0;
}